<%= render 'layouts/header' %>

<div class="span3 sidebar-nav">
  <%= render 'shared/menu_manage' %>
</div>

<div class="span8">
  <p id="notice"><%= notice %></p>

  <p>
    <b>Title:</b>
    <%= @notice.title %>
  </p>

  <p>
    <b>Content:</b>
    <%= @notice.content %>
  </p>

  <%= link_to 'editar', edit_notice_path(@notice) %> |
  <%= link_to 'voltar', notices_path %>
</div>